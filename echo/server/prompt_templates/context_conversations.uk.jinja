{% if is_overview_mode %}РЕЗЮМЕ РОЗМОВ:{% else %}ТРАНСКРИПЦІЇ РОЗМОВ:{% endif %}
{% if conversations|length == 0 %}
Користувач ще не додав жодної розмови. М'яко запропонуйте йому додати деякі, обравши розмови з бічної панелі.
{% else %}
   {% for conversation in conversations %}
<conversation>
   <name>{{ conversation.name }}</name>
   <tags>{{ conversation.tags }}</tags>
   <date>{{ conversation.created_at }}</date>
   <duration>{{ conversation.duration }}</duration>
   {% if conversation.summary %}
   <summary>
   {{ conversation.summary }}
   </summary>
   {% else %}
   <transcript>
   {{ conversation.transcript }}
   </transcript>
   {% endif %}
   {% if conversation.artifacts %}
   <artifacts>
      {% for artifact in conversation.artifacts %}
      <artifact>
         <key>{{ artifact.key }}</key>
         <content>{{ artifact.content }}</content>
      </artifact>
      {% endfor %}
   </artifacts>
   {% endif %}
</conversation>
   {% endfor %}
{% endif %}
