{% if is_overview_mode %}RESÚMENES DE CONVERSACIONES:{% else %}TRANSCRIPCIONES DE CONVERSACIONES:{% endif %}
{% if conversations|length == 0 %}
No hay conversaciones añadidas por el usuario todavía. Anímelos suavemente a agregar algunas seleccionando conversaciones de la barra lateral.
{% else %}
   {% for conversation in conversations %}
<conversation>
   <name>{{ conversation.name }}</name>
   <tags>{{ conversation.tags }}</tags>
   <date>{{ conversation.created_at }}</date>
   <duration>{{ conversation.duration }}</duration>
   {% if conversation.summary %}
   <summary>
   {{ conversation.summary }}
   </summary>
   {% else %}
   <transcript>
   {{ conversation.transcript }}
   </transcript>
   {% endif %}
   {% if conversation.artifacts %}
   <artifacts>
      {% for artifact in conversation.artifacts %}
      <artifact>
         <key>{{ artifact.key }}</key>
         <content>{{ artifact.content }}</content>
      </artifact>
      {% endfor %}
   </artifacts>
   {% endif %}
</conversation>
   {% endfor %}
{% endif %}
